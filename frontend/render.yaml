services:
  - type: static
    name: vivaju-front
    env: static
    buildCommand: pnpm build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html